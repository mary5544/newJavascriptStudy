/*
  >> 배열에 접근하기
  ->
    let color = [ 'rad', 'orange', 'green', 'yellow' ];
    console.log(color[0]) // rad
    console.log(color[1]) // orange
    ...
    console.log(color[0][0]) // r
    console.log(color[0][1]) // a
  <-

  >> 배열 내용수정
  ->
    let color = [ 'rad', 'orange', 'green', 'yellow' ];
    color[0] = 'red'
    console.log(color[0]); // red
  <-

  >> 배열 추가
  ->
    let color = [ 'rad', 'orange', 'green', 'yellow' ];
    color[4] = 'blue'
    console.log(color[4]); // blue
  <-
*/